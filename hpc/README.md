# HPC workflows

The canonical Picasso workflow is versioned under:

```text
hpc/picasso/
```

On Picasso, `$HOME/pyexpint_toms/ops` is a symbolic link to this directory.
Do not maintain a second independent copy of these scripts.
